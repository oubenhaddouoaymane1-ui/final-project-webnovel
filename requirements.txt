# ═══════════════════════════════════════════════════════════════════════════════
# CineOS — Python Dependencies (Cloud-First, Local Controller)
# No heavy ML libraries — all AI runs on cloud services
# ═══════════════════════════════════════════════════════════════════════════════

# Telegram
python-telegram-bot==22.8

# HTTP client (cloud API calls)
httpx==0.27.0

# Database
asyncpg==0.29.0

# Redis
redis==5.0.7

# Web framework (workers + health endpoints)
fastapi==0.111.0
uvicorn==0.30.1
starlette==0.37.4

# Configuration
pyyaml==6.0.1
python-dotenv==1.0.1
pydantic==2.7.4

# Security
cryptography==42.0.8
jsonschema==4.22.0

# Edge TTS (free Microsoft TTS)
edge-tts==6.1.12

# Audio processing (lightweight)
pydub==0.25.1

# Image processing (lightweight only)
Pillow==10.3.0

# Utilities
tqdm==4.66.4
structlog==24.2.0

# Development / Testing
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
ruff==0.4.8
mypy==1.10.1
